## Summary

<!-- What changed and why? -->

## Test plan

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes (landing page)
- [ ] `npm run build` succeeds
- [ ] `npm run ci:static` passes
- [ ] If API/auth touched: server running + `npm run test:auth` / `npm run test:smoke`
- [ ] No secrets or personal data included in this PR

## Checklist

- [ ] Changes are scoped to the described problem
- [ ] `.env` and runtime data files are not committed
- [ ] If user-facing: `CHANGELOG.md` and `docs/changelog.md` stay in sync
